{
    "name": "@dotnet/kiotajs",
    "type": "module",
    "main": "dist/main.js",
    "scripts": {
        "clean": "rm -rf dist bin obj",
        "build": "dotnet build --configuration Release",
        "start": "(cd dist && python3 -m http.server)"
    }
}
